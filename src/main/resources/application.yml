spring:
  datasource:
    url: ${SPRING_DATASOURCE_URL:jdbc:postgresql://localhost:5432/nghenhac}
    username: ${SPRING_DATASOURCE_USERNAME:postgres}
    password: ${SPRING_DATASOURCE_PASSWORD:postgres}
    driver-class-name: org.postgresql.Driver
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        dialect: org.hibernate.dialect.PostgreSQLDialect

minio:
  endpoint: ${MINIO_ENDPOINT:http://localhost:9000}
  access-key: ${MINIO_ACCESS_KEY:minioadmin}
  secret-key: ${MINIO_SECRET_KEY:minioadmin}
  bucket:
    songs: ${MINIO_BUCKET_SONGS:songs}
    covers: ${MINIO_BUCKET_COVERS:covers}

jwt:
  secret: ${JWT_SECRET:9aF3cK2sP7qR8vW4xY0zB1aC5dE6fG7hI8jK9lM0nO1pQ2rS3tU4vW5xY6zA7bC8}
